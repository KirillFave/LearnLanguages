@using WebApp.ViewModels.Schemes

@model SchemeVM

<div class="grid-container">
    @for (int row = 1; row <= 9; row++)
    {
        @for (int column = 1; column <= 15; column++)
        {
            string cellContent = $"{(char)('A' + column - 1)}{row}";

            <div class="grid-item scheme-item"
                 data-row="@row"
                 data-column="@column">
                @cellContent
            </div>
        }
    }
</div>